## Artifacts

本目录用于存放 **生成物/导出物**（例如 Lighthouse 报告、截图、导出的 JSON/HTML）。

### 快速入口

- **Lighthouse 产物**：`artifacts/lighthouse/`

### 维护规则

- 这里的文件通常是 **可再生成** 的导出结果，尽量不要手工编辑其内容。
- 若需要说明生成方式，把说明写到对应课程报告或 `coursework/docs/guides/` 中，而不是改导出文件本体。
