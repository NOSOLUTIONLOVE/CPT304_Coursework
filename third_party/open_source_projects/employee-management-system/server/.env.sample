# Copy this file to .env and adjust if needed.
# DATABASE_URL: MongoDB connection string WITHOUT the database name (the app appends /EMS).
# Local default:
DATABASE_URL=mongodb://127.0.0.1:27017
# API port (must match frontend baseURL, default 8000)
PORT=8000
